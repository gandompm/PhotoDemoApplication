package com.example.photodemoapplication.common

/**
 *  defining Constants as object
 *
 *  object is a special class that only has one instance.
 *  Kotlin makes a static reference for the object, and initializes the reference in a static block.
 */
object Constants {

    const val BASE_URL = "https://pixabay.com/api/"
    const val API_KEY = "36014268-fb45f776880a5f3f55fd690fa"

    const val MIN_SEARCH_QUERY_LENGTH = 3
}